
function sequencia(){
    var num = Number(window.prompt("Digite um valor qualquer"));
    var prox = num + 1;
    var ant = num - 1;
    window.alert(`Antes de ${num} temos ${ant} e depois temos ${prox}`);
}